var core = require('./core.js');

var obj = {};

obj.than = function () {
    var args        = Array.prototype.slice.call(arguments),
        func        = args.reverse().shift(),
        mainFunc    = this;
    setTimeout(function() {
        func(mainFunc.apply(mainFunc,args.reverse()));
    });
};
core.addProperties(Function.prototype, obj);