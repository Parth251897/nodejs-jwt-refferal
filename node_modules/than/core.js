'use strict';

exports.addProperties = function(original, extension) {
    for (var key in extension) {
        (!original.hasOwnProperty(key)) && Object.defineProperty(original, key, {
            value: extension[key],
            writable: true
        });
    }
};

